Sources for the plugin `plugin.appodeal.InnerActive`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.InnerActive" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
